module.exports = {
  plugins: {
    // Tailwind v4: use the dedicated PostCSS plugin (includes autoprefixer-like
    // vendor prefixing via Lightning CSS — no separate autoprefixer needed).
    '@tailwindcss/postcss': {},
  },
};
